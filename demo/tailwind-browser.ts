// @tailwindcss/browser is a self-executing IIFE that uses MutationObserver
// to compile Tailwind classes on the fly. Importing it registers the observer.
// It looks for <style type="text/tailwindcss"> in the document.
import '@tailwindcss/browser';

